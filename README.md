# Ryan02I5-Labs `.github`

This repository owns the GitHub defaults for `Ryan02I5-Labs`.

It is the official place for:

- organization profile content
- default community health files
- default cross-repository contribution and agent guidance
- workflow templates for new repositories

It is not the runtime home for active reusable workflows. Those stay in
`Ryan02I5-Labs/github-automation`.
